name: Build NixOS hosts

# Validate that every host's system closure builds. This runs on PRs (the gate
# before merge) and on pushes to main (the source of truth hosts pull from).
on:
  pull_request:
  push:
    branches: [main]

# Cancel superseded runs on the same ref to save CI minutes.
concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: build ${{ matrix.host }} (${{ matrix.system }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - host: rgpeach10-mini
            system: x86_64-linux
          - host: rgpeach10-pi1
            system: aarch64-linux
    steps:
      - uses: actions/checkout@v4

      # aarch64 hosts are built under emulation on the x86_64 runner.
      - name: Enable aarch64 emulation
        if: matrix.system == 'aarch64-linux'
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - uses: cachix/install-nix-action@v30
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            extra-platforms = aarch64-linux

      - name: Build ${{ matrix.host }} system closure
        run: |
          nix build -L --out-link result \
            ".#nixosConfigurations.${{ matrix.host }}.config.system.build.toplevel"

      # Fast PR-time CVE scan: reuses the closure we just built (no rebuild),
      # so the only added cost is the NVD feed fetch. Non-blocking — a NixOS
      # closure almost always carries some open CVEs, so this reports findings
      # without gating the merge. Tighten with a vulnix whitelist if desired.
      - name: Scan closure for known CVEs (vulnix)
        continue-on-error: true
        run: nix run nixpkgs#vulnix -- --requisites ./result
